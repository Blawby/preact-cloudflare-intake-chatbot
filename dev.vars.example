# Local development environment variables (Worker Secrets Only)
# Copy this file to .dev.vars and fill in your actual values

# Environment Configuration
NODE_ENV=development

# Authentication Configuration
# Base URL for OAuth redirects - required for production, optional for development
# In development: set in .dev.vars (this file)
# In production: set via wrangler secrets (see wrangler.toml for commands)
BETTER_AUTH_URL=http://localhost:8787

# Google OAuth Configuration (SENSITIVE)
# In development: set in .dev.vars (this file)  
# In production: set via wrangler secrets
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Test User Configuration (Development Only)
# These credentials are used for testing authenticated pages in development
# NOTE: These are fake credentials for testing only - do not use real credentials
TEST_USER_EMAIL=dev-test-user@fake-test-domain.invalid
TEST_USER_PASSWORD=dev-test-password-123-fake
TEST_USER_NAME=Dev Test User

# Debug Configuration
VITE_DEBUG_OVERLAY=false

# API Keys and Tokens (SENSITIVE)
BLAWBY_API_TOKEN=your_blawby_api_token_here
LAWYER_SEARCH_API_KEY=your_lawyer_search_api_key_here
CLOUDFLARE_API_TOKEN=your_cloudflare_api_token_here
RESEND_API_KEY=your_resend_api_key_here

# Account IDs (SENSITIVE)
CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id_here
